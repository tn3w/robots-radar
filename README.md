# robots-radar
Tracks which crawlers the web's top sites block. Fetches robots.txt from the Tranco top-1M, parses allow/disallow rules per user-agent, and publishes a per-domain block map plus a time series of block rates per crawler.
